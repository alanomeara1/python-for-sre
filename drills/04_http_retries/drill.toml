title = "HTTP with retries: backoff, 5xx/429, Retry-After"
target_minutes = 20
variant_target_minutes = 15
patterns = ["requests.Session().get(url, timeout=...)", "retry loop with for attempt in range(1, n + 1)", "exponential backoff capped, optional jitter", "retry 5xx/429 and connection errors, not other 4xx", "Retry-After header", "injectable sleep for testability", "decorator factory with functools.wraps"]
